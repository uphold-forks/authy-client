
/**
 * Module dependencies.
 */

import activity from './activity-enum';
import locale from './locale-enum';
import resolution from './resolution-enum';
import verificationVia from './verification-via-enum';

/**
 * Export named enums.
 */

export {
  activity,
  locale,
  resolution,
  verificationVia
};
